# ==========================================
# CLAW ARENA - Contract Deployment Config
# ==========================================
# Copy this to .env and fill in your values:
#   cp .env.example .env

# ------------------------------------------
# DEPLOYER WALLET (Required)
# ------------------------------------------
# This wallet deploys and owns the ArenaFactory contract.
# It needs testnet MON for gas fees.
#
# How to set up:
#   1. Open MetaMask (or any wallet)
#   2. Use an existing account or create a new one
#   3. Add Monad Testnet: RPC https://testnet-rpc.monad.xyz, Chain ID 10143
#   4. Get free testnet MON from https://testnet.monad.xyz/
#   5. Export the private key: MetaMask > Account > Export Private Key
#   6. Paste it below (starts with 0x)
#
DEPLOYER_PRIVATE_KEY=

# ------------------------------------------
# OPERATOR ADDRESS (Required)
# ------------------------------------------
# This is the agent's signing wallet. It signs EIP-712 messages to
# authorize tournament finalization. It does NOT need any MON.
#
# How to set up:
#   1. Create a NEW separate wallet in MetaMask (Account > Create Account)
#   2. Copy the PUBLIC address (0x...) and paste below
#   3. Copy the PRIVATE KEY and put it in backend/.env as OPERATOR_PRIVATE_KEY
#
# The public address goes here, the private key goes in backend/.env
#
OPERATOR_ADDRESS=

# ------------------------------------------
# TREASURY ADDRESS (Required)
# ------------------------------------------
# Receives protocol fees (2-3%) when tournaments finalize.
# For testing, you can use your deployer wallet address.
# For production, use a multisig or separate wallet.
#
TREASURY_ADDRESS=

# ------------------------------------------
# ETHERSCAN API KEY (Optional)
# ------------------------------------------
# For verifying contracts on Monadscan (Etherscan-powered).
# NOT required - Sourcify verification works without it.
# If you want it: sign up at https://etherscan.io, go to API Keys.
#
# ETHERSCAN_API_KEY=

# ------------------------------------------
# Arena Creation Defaults (Optional)
# ------------------------------------------
# Used by scripts/create-arena.js

# ARENA_NAME="Claw Arena Tournament #1"
# ARENA_ENTRY_FEE=0.01
# ARENA_MAX_PLAYERS=8
# ARENA_PROTOCOL_FEE=250
# ARENA_REG_DEADLINE=60
